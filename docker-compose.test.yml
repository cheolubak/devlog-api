services:
  test-db:
    image: postgres:15-alpine
    container_name: devlog-test-db
    environment:
      POSTGRES_DB: devlog_test
      POSTGRES_PASSWORD: testpass
      POSTGRES_USER: testuser
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data
